import React from 'react';
import { ProviderDashboard } from '@/components/features/provider/ProviderDashboard/ProviderDashboard';

export const ProfessionalDashboard: React.FC = () => {
  return <ProviderDashboard />;
};

